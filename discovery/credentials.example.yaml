# OAP Tool Bridge Credentials
# Copy to credentials.yaml and fill in your API keys.
# credentials.yaml is gitignored — never commit real keys.
#
# Keys are matched by manifest domain. The auth type (api_key, bearer)
# and header name (auth_name) come from the manifest's invoke spec.

# Example: API key auth (manifest has auth: "api_key", auth_name: "X-API-Key")
# mynewscast.org:
#   key: "your-api-key-here"

# Example: Bearer token auth (manifest has auth: "bearer")
# api.example.com:
#   key: "your-bearer-token-here"

# Example: Kagi search (manifest has auth: "bot")
# Get your API key from https://kagi.com/settings/api
# kagi.com:
#   key: "your-kagi-api-key-here"

# Example: Tavily web search (manifest has auth: "bearer")
# Sign up at https://tavily.com, get API key from dashboard
# api.tavily.com:
#   key: "tvly-your-api-key-here"
