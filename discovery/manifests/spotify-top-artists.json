{
  "oap": "1.0",
  "name": "spotify-top-artists",
  "description": "Get the user's top Spotify artists by listening time. Returns artist names, genres, and popularity scores. Use for questions like 'what artists have I been listening to?', 'who are my favorite artists?', 'what genres do I listen to most?', 'what's my music taste?'. time_range controls the window: short_term=last 4 weeks, medium_term=last 6 months, long_term=all time.",
  "input": {
    "format": "application/json",
    "description": "time_range controls the window: short_term (4 weeks), medium_term (6 months, default), long_term (all time). limit is 1-50 (default: 20).",
    "parameters": {
      "time_range": {"type": "string", "description": "short_term (4 weeks), medium_term (6 months, default), long_term (all time)", "required": false},
      "limit": {"type": "integer", "description": "Number of artists 1-50 (default: 20)", "required": false}
    }
  },
  "output": {
    "format": "application/json",
    "description": "items array of artist objects, each with name, genres (array), popularity (0-100), followers count, and Spotify ID."
  },
  "invoke": {
    "method": "GET",
    "url": "http://localhost:8306/proxy/me/top/artists"
  },
  "usage": "GET /proxy/me/top/artists?time_range=medium_term&limit=20",
  "tags": ["spotify", "music", "artists", "taste", "genres", "listening"]
}
