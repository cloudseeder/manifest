{
  "oap": "1.0",
  "name": "spotify-recently-played",
  "description": "Get the user's recently played Spotify tracks with timestamps. Use for questions like 'what have I been listening to?', 'what did I play recently?', 'what was I listening to this morning?', 'show my recent listening history'. Returns up to 50 of the most recently played tracks.",
  "input": {
    "format": "application/json",
    "description": "limit is 1-50 (default: 20). Returns tracks played most recently first.",
    "parameters": {
      "limit": {"type": "integer", "description": "Number of tracks 1-50 (default: 20)", "required": false}
    }
  },
  "output": {
    "format": "application/json",
    "description": "items array where each item has track (name, artists, album) and played_at (ISO 8601 timestamp)."
  },
  "invoke": {
    "method": "GET",
    "url": "http://localhost:8306/proxy/me/player/recently-played"
  },
  "usage": "GET /proxy/me/player/recently-played?limit=20",
  "tags": ["spotify", "music", "recently-played", "history", "listening"]
}
